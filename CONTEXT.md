# 小说悦读

把任意小说网页重排为本地沉浸阅读视图的浏览器扩展。本文件只记录这个上下文里的领域术语。

## Language

**书籍（Book）**：
一部可供连续阅读的作品，是阅读进度的归属单位。
_Avoid_: 小说、书目

**章节（Chapter）**：
书籍中可独立加载与阅读的一页正文及其导航信息。
_Avoid_: 页面、文章

**目录（Catalog）**：
一本书的章节清单，用于跳转到任意章节。
_Avoid_: 书页、索引

**书键（Book Key）**：
一本书的稳定身份，用于把阅读进度与书关联，由目录页地址或章节地址推导。
_Avoid_: bookId、书名

**阅读模式（Reading Mode）**：
接管当前页面、以重排正文替代原站排版的整页状态。
_Avoid_: 沉浸模式、阅读器（阅读器指实现，见页面层守卫）

**正文提取管线（Content Extraction Pipeline）**：
把任意网页归约为章节正文的三层流程：站点规则选择器、通用正文提取、中文标点密度启发式。
_Avoid_: 解析器、抓取器

**简繁转换（Text Conversion）**：
把正文与标题在简体、繁体字形之间变换后再呈现，抓取到的原文保持不变。
_Avoid_: 繁简转换、字形替换

**端侧翻译（On-device Translation）**：
用浏览器本地翻译能力把正文改写或并列呈现为另一种语言，原文仍保留在章节记录中。
_Avoid_: 机翻、云端翻译

**章节窗口（Chapter Window）**：
阅读模式中当前连续拼接、保留完整正文与 DOM 的相邻章节集合。
_Avoid_: 缓存、章节列表

**阅读进度（Reading Progress）**：
一本书上一次读到哪一章节、以及章节内哪个位置。
_Avoid_: 书签、历史

**跳转意图（Navigation Intent）**：
一次跨页面跳转在落地页自动进入阅读模式、并说明是否恢复位置的约定。
_Avoid_: 自动打开标记、跳转标记

**站点启停（Site Enablement）**：
用户对某个站点的开关，关闭后该站点不出现阅读入口。持久化键沿用历史名 `blacklist`（冻结，见 ADR-0001），不代表领域术语。
_Avoid_: 白名单

**网络层防护（Network-layer Protection）**：
阅读模式开启期间，在请求发出前只放行本站域名的拦截。
_Avoid_: 广告屏蔽、DNR 规则

**页面层守卫（Page-layer Guard）**：
阅读模式开启期间，在页面内阻止站点脚本跳转、弹窗、篡改地址栏与摘除阅读视图。
_Avoid_: 导航锁定（那只是其中一部分）

**阅读器保活不变量（Reader Keep-alive Invariant）**：
阅读模式一旦进入，站点脚本不得使阅读视图卸载或离开当前文档。
_Avoid_: 防退出
